# Sensitive files
forms/config.php

# Environment variables (API keys, secrets)
.env
.env.local
.env.*.local

# Build outputs (injected keys stay local)
dist/
build/

# Node modules (if using npm for Vite/dotenv)
node_modules/
npm-debug.log*

# Composer dependencies (if adding PHP packages like PHPMailer)
vendor/
composer.lock  # Optional: Ignore if you want reproducible builds; otherwise, commit it

# Local logs and caches (PHP errors, sessions, etc.)
*.log
logs/
tmp/
cache/

# OS generated files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/
.vscode/settings.json  # Extra: Ignore workspace-specific settings
*.swp
*.swo  # Vim temp files

# PDF/Email Tool Extras (new: for temp outputs from TCPDF/Dompdf or PHPMailer attachments)
*.pdf  # Ignore generated PDFs; commit only templates (e.g., employment-template.pdf)
PHPMailer/attachments/  # If testing attachments locally